spring:
  datasource:
    url: jdbc:h2:mem:test
    username: sa
    password:
    driver-class-name: org.h2.Driver
  #S3
  cloud:
    aws:
      credentials:
        access-key: test
        secret-key: test
      region:
        static: test # bucket region
      s3:
        bucket: test # bucket 명
#  # REDIS
#  data:
#    redis:
#      host: localhost
#      port: 6379
#
#    # MONGO
#    mongodb:
#      uri: mongodb://localhost:27017/test
