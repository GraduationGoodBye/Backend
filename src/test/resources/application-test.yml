spring:
  # MYSQL
  datasource:
    url: jdbc:mysql://localhost:3306/ggbTest?useSSL=false&allowPublicKeyRetrieval=true
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: root
  #S3
  cloud:
    aws:
      credentials:
        access-key: ggb
        secret-key: ggb
      region:
        static: ggb # bucket region
      s3:
        bucket: ggb # bucket 명
  security:
    oauth2:
      client:
        registration:
          google:
            client-id: ggb
            client-secret: ggb
            scope:
jwt:
  secret: ggggggggggggggggggggggbbbbbbbbbbbbbbbbbbbbbb
  expired:
    access: 123
    refresh: 123

#  # REDIS
#  data:
#    redis:
#      host: localhost
#      port: 6379
#
#    # MONGO
#    mongodb:
#      uri: mongodb://localhost:27017/test